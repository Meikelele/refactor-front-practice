import './ConfirmButton.scss';

export default function ConfirmButton() {

    return (
        <div className="confirmButton">
            <button>
                Confirm
            </button>
        </div>
    );
}